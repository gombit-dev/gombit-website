# Config

Application configuration is the process environment, loaded by
`config.Load()` in `cmd/server`. Copy `../.env.example` to `../.env` and
adjust values. Do not put secrets in `VITE_*` variables.
